# Production dependencies for nano-vllm-voxcpm + deployment service.
# Install with:  pip install -r requirements.txt
#
# Pinned to avoid the flash-attn from-source compile. The flash-attn wheel
# below must match: torch version, CUDA series, Python version, cxx11abi flag.

--extra-index-url https://download.pytorch.org/whl/cu126

# Torch family (cu126 prebuilt wheels)
torch==2.7.1
torchaudio==2.7.1

# Engine deps
triton>=3.0.0
transformers>=4.51.0
xxhash
tqdm
numpy
librosa
pydantic
soundfile>=0.13.1
torchcodec

# Prebuilt flash-attn wheel — skips the 30-90 min from-source compile.
# Matches: cu12 + torch 2.7 + cp310 + cxx11abi=FALSE + linux_x86_64.
# If you change the torch/python/cuda version above, regenerate this URL from:
#   https://github.com/Dao-AILab/flash-attention/releases/tag/v2.8.3
https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.7cxx11abiFALSE-cp310-cp310-linux_x86_64.whl

# FastAPI service deps
fastapi[standard]
aiohttp
prometheus_client
lameenc
boto3
huggingface_hub
